# Example: Syncing Database Model Documentation

## Scenario
Developer добавил новый столбец `financial_center_ids` в таблицу `t_d_article`, но забыл обновить documentation.

## Code Changes
```python
# backend/app/models/article.py

class Article(Base):
    __tablename__ = "t_d_article"

    id = Column(Integer, primary_key=True)
    name = Column(String(255), nullable=False)
    type = Column(Enum("income", "expense"), nullable=False)
    parent_id = Column(Integer, ForeignKey("t_d_article.id"))
    is_active = Column(Boolean, default=True)

    # NEW COLUMN - Added for financial center filtering
    financial_center_ids = Column(ARRAY(Integer), nullable=True)
```

## Migration Created
```python
# backend/db/migrations/versions/20260209_add_fc_to_articles.py

def upgrade():
    op.add_column('t_d_article',
        sa.Column('financial_center_ids', sa.ARRAY(sa.Integer()), nullable=True)
    )
    op.create_index(
        'idx_article_fc_gin',
        't_d_article',
        ['financial_center_ids'],
        postgresql_using='gin'
    )

def downgrade():
    op.drop_index('idx_article_fc_gin')
    op.drop_column('t_d_article', 'financial_center_ids')
```

## Git Commit
```bash
git commit -m "feat(db): add financial center filtering to articles

Added financial_center_ids array column to t_d_article for filtering
categories by financial centers. Includes GIN index for array queries."
```

## Doc-Sync Execution

### Step 1: Git Diff Analysis
```bash
$ @skill:doc-sync

🔍 Analyzing git diff...

Changed files:
  ✓ backend/app/models/article.py
  ✓ backend/db/migrations/versions/20260209_add_fc_to_articles.py

Component detection:
  🔄 MODIFIED database model: t_d_article
     Source: backend/app/models/article.py:45
     Changes: Added column 'financial_center_ids'

  ➕ NEW migration: 20260209_add_fc_to_articles
     Source: backend/db/migrations/versions/20260209_add_fc_to_articles.py
```

### Step 2: Architecture Refs Mapping
```
🔗 Mapping to documentation...

Found architecture_refs in skills:
  - db-management → docs/architecture/database/dimensions.yaml

Affected documentation:
  📄 docs/architecture/database/dimensions.yaml#/tables/t_d_article (NEEDS UPDATE)
  📄 docs/architecture/database/indexes.yaml (MAY NEED UPDATE - new index)
```

### Step 3: Code Signature Extraction
```
📝 Extracting model schema...

Extracted columns:
  - id: Integer, primary_key=True
  - name: String(255), nullable=False
  - type: Enum('income', 'expense'), nullable=False
  - parent_id: Integer, ForeignKey(t_d_article.id), nullable=True
  - is_active: Boolean, default=True, nullable=True
  - financial_center_ids: ARRAY(Integer), nullable=True  ⬅️ NEW

Extracted indexes:
  - idx_article_fc_gin: GIN index on financial_center_ids  ⬅️ NEW
```

### Step 4: Staleness Detection
```
⚠️ Staleness Issues Found:

1. Missing column in documentation
   Severity: HIGH
   Component: t_d_article.financial_center_ids
   File: docs/architecture/database/dimensions.yaml
   Issue: Column exists in model but not documented

2. Missing index in documentation
   Severity: MEDIUM
   Component: idx_article_fc_gin
   File: docs/architecture/database/indexes.yaml
   Issue: Index created in migration but not documented
```

### Step 5: YAML Auto-Update Generation

**For dimensions.yaml:**
```yaml
✨ Generated YAML snippet for t_d_article columns:

  - name: financial_center_ids
    type: integer[]
    constraints: []
    description: |
      Array of financial center IDs for filtering categories.
      Allows restricting article visibility to specific financial centers.
      NULL means visible to all financial centers.

      Auto-generated by doc-sync on 2026-02-09 20:15:30
      Source: backend/app/models/article.py:45

      TODO: Verify business logic
      TODO: Document use cases
```

**For indexes.yaml:**
```yaml
✨ Generated YAML snippet for indexes:

  - name: idx_article_fc_gin
    table: t_d_article
    columns: [financial_center_ids]
    type: gin
    description: |
      GIN index for efficient array queries on financial_center_ids.
      Supports queries like: WHERE financial_center_ids && ARRAY[1,2,3]

      Auto-generated by doc-sync on 2026-02-09 20:15:30
      Source: backend/db/migrations/versions/20260209_add_fc_to_articles.py:10

      TODO: Add performance benchmarks
```

### Step 6: User Approval
```
❓ Preview update for docs/architecture/database/dimensions.yaml

   Will add:
   - Column 'financial_center_ids' to t_d_article

   Preview:
   [YAML snippet above]

Apply this update? (yes/no/edit)
> yes

❓ Preview update for docs/architecture/database/indexes.yaml

   Will add:
   - Index 'idx_article_fc_gin' on t_d_article

Apply this update? (yes/no/edit)
> yes
```

### Step 7: Apply Updates
```
✅ Applying updates...

Updated files:
  📝 docs/architecture/database/dimensions.yaml
     Added column: financial_center_ids to t_d_article (10 lines)

  📝 docs/architecture/database/indexes.yaml
     Added index: idx_article_fc_gin (8 lines)
```

### Step 8: Changelog Generation
```
📋 Generating changelog entry...

Added to docs/architecture/README.md:

### 2026-02-09: Add financial center filtering to articles ⭐ MAJOR

- **Change:** Added financial_center_ids array column to t_d_article for filtering categories by financial centers
- **Significance:** MAJOR
- **Modified files:**
  - backend/app/models/article.py
  - backend/db/migrations/versions/20260209_add_fc_to_articles.py
- **Updated documentation:**
  - docs/architecture/database/dimensions.yaml
  - docs/architecture/database/indexes.yaml
- **Components affected:**
  - Database tables: t_d_article (new column)
  - Indexes: idx_article_fc_gin (GIN index)
  - Migrations: 20260209_add_fc_to_articles

**Migration notes:**
- Nullable column - safe to deploy without downtime
- GIN index creation may take 10-30 seconds on large tables
- No application code changes required (backward compatible)

---
```

### Step 9: Git Commit
```
🔧 Creating documentation commit...

git add docs/architecture/database/dimensions.yaml
git add docs/architecture/database/indexes.yaml
git add docs/architecture/README.md
git commit -m "docs: sync database schema for article filtering

- Added financial_center_ids column documentation
- Documented idx_article_fc_gin GIN index
- Updated changelog with migration notes"

[dev/add_fc_filtering_20260209201530 def5678] docs: sync database schema for article filtering
 3 files changed, 25 insertions(+)
```

## Final Output
```
✅ Documentation sync complete!

Summary:
  📊 Changed files: 2
  📄 Updated docs: 2
  📝 Changelog entries: 1
  💾 Commits: 1

Next steps:
  1. Review updated YAML files
  2. Complete TODO items:
     - Verify business logic for financial_center_ids
     - Document use cases
     - Add performance benchmarks for GIN index
  3. Run '@skill:pr-automation' to create PR
```

## Manual Review Tasks

Developer should complete these TODOs:

### 1. Update dimensions.yaml
```yaml
# BEFORE (auto-generated):
- name: financial_center_ids
  type: integer[]
  description: |
    Array of financial center IDs for filtering categories.
    TODO: Verify business logic
    TODO: Document use cases

# AFTER (manual review):
- name: financial_center_ids
  type: integer[]
  description: |
    Array of financial center IDs for filtering categories.

    Business logic:
    - NULL: Category visible to all financial centers (default)
    - Empty array []: Category hidden from all FCs (rare)
    - Array [1,2,3]: Category visible only to specified FCs

    Use cases:
    - Regional expense categories (e.g., "Travel - Europe" only for EU FCs)
    - Department-specific categories (e.g., "IT Equipment" only for IT FC)
    - Multi-entity businesses with separate chart of accounts

    Query examples:
    - WHERE financial_center_ids IS NULL OR 1 = ANY(financial_center_ids)
    - WHERE financial_center_ids && ARRAY[1,2,3]  (uses GIN index)
```

### 2. Update indexes.yaml
```yaml
# BEFORE (auto-generated):
- name: idx_article_fc_gin
  type: gin
  description: |
    GIN index for efficient array queries.
    TODO: Add performance benchmarks

# AFTER (manual review):
- name: idx_article_fc_gin
  type: gin
  description: |
    GIN index for efficient array queries on financial_center_ids.

    Performance benchmarks (1000 categories, 10% with FC filters):
    - Without index: ~150ms for array overlap query
    - With GIN index: ~5ms for array overlap query (30x faster)

    Index size: ~50KB (estimated)
    Build time: ~10-30 seconds on production

    Supported operators:
    - && (array overlap)
    - @> (array contains)
    - <@ (array is contained by)
```

## Testing Checklist
After sync, verify:
- [ ] Migration applied successfully
- [ ] Index created (check `\d t_d_article` in psql)
- [ ] Documentation accurate (review YAML files)
- [ ] Changelog entry complete
- [ ] No broken $ref links
- [ ] Business logic documented
- [ ] Performance benchmarks added (if applicable)
