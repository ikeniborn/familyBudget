# Example: Syncing API Endpoint Documentation

## Scenario
Developer добавил новый endpoint для поиска статей, но забыл обновить документацию.

## Code Changes
```python
# backend/app/api/v1/endpoints/articles.py

@router.get("/articles/search", response_model=ArticleListResponse)
async def search_articles(
    q: str = Query(..., description="Search query"),
    type: Optional[str] = Query(None, description="Filter by type"),
    current_user: User = Depends(get_current_user)
):
    """
    Full-text search for articles.

    Returns paginated list of articles matching the search query.
    """
    return await ArticleService.search(q=q, type=type)
```

## Git Commit
```bash
git commit -m "feat(api): add article search endpoint

Implemented full-text search with query filtering by type.
Supports pagination with default limit=50."
```

## Doc-Sync Execution

### Step 1: Git Diff Analysis
```bash
$ @skill:doc-sync

🔍 Analyzing git diff...

Changed files:
  ✓ backend/app/api/v1/endpoints/articles.py

Component detection:
  ➕ NEW API endpoint: GET /api/v1/articles/search
     Source: backend/app/api/v1/endpoints/articles.py:145
     Decorator: @router.get("/articles/search")
```

### Step 2: Architecture Refs Mapping
```
🔗 Mapping to documentation...

Found architecture_refs in skills:
  - api-development → docs/architecture/endpoints/articles.yaml

Affected documentation:
  📄 docs/architecture/endpoints/articles.yaml (NEEDS UPDATE)
  📄 docs/architecture/functionality/budget-management.yaml (MAY NEED UPDATE)
```

### Step 3: Code Signature Extraction
```
📝 Extracting endpoint signature...

Extracted:
  Path: /api/v1/articles/search
  Method: GET
  Auth: user (via get_current_user dependency)
  Query params:
    - q: string (required) - "Search query"
    - type: string (optional) - "Filter by type"
  Response: ArticleListResponse
  Description: "Full-text search for articles."
```

### Step 4: Staleness Detection
```
⚠️ Staleness Issues Found:

1. Missing component
   Severity: HIGH
   Component: GET /api/v1/articles/search
   File: docs/architecture/endpoints/articles.yaml
   Issue: Endpoint exists in code but not documented

   Recommendation: Add endpoint documentation
```

### Step 5: YAML Auto-Update Generation
```
✨ Generated YAML snippet:

# ---------------------------------------------------------------------------
# SEARCH (NEW - Auto-detected)
# ---------------------------------------------------------------------------
- path: "/api/v1/articles/search"
  method: GET
  auth: user
  description: "Full-text search for articles"
  query_params:
    q: "string - search query (required)"
    type: "income | expense (optional) - filter by type"
  response:
    status: 200
    body:
      articles: "array of article objects"
      total: "integer - total count"
      limit: "integer - page size"
      offset: "integer - skip count"
  service:
    $ref: "../functionality/budget-management.yaml#/module/services/1"
  notes: |
    Auto-generated by doc-sync on 2026-02-09 19:45:30
    Source: backend/app/api/v1/endpoints/articles.py:145

    Please review:
    - [ ] Description is accurate
    - [ ] Service reference is correct
    - [ ] Response schema is complete
    - [ ] Error responses documented
```

### Step 6: User Approval
```
❓ Preview update for docs/architecture/endpoints/articles.yaml

   Will add:
   - GET /api/v1/articles/search route definition

   Location: After existing /api/v1/articles routes

Apply this update? (yes/no/edit)
> yes
```

### Step 7: Apply Updates
```
✅ Applying updates...

Updated files:
  📝 docs/architecture/endpoints/articles.yaml
     Added route: GET /api/v1/articles/search (15 lines)
```

### Step 8: Changelog Generation
```
📋 Generating changelog entry...

Added to docs/architecture/README.md:

### 2026-02-09: Add article search endpoint ⭐ MAJOR

- **Change:** Implemented full-text search for articles with query parameter filtering
- **Significance:** MAJOR
- **Modified files:**
  - backend/app/api/v1/endpoints/articles.py
- **Updated documentation:**
  - docs/architecture/endpoints/articles.yaml
- **Components affected:**
  - API endpoints: GET /api/v1/articles/search

---
```

### Step 9: Git Commit
```
🔧 Creating documentation commit...

git add docs/architecture/endpoints/articles.yaml
git add docs/architecture/README.md
git commit -m "docs: sync endpoint documentation for article search

- Added GET /api/v1/articles/search route
- Updated changelog with MAJOR significance"

[dev/add_article_search_20260209194530 abc1234] docs: sync endpoint documentation for article search
 2 files changed, 20 insertions(+)
```

## Final Output
```
✅ Documentation sync complete!

Summary:
  📊 Changed files: 1
  📄 Updated docs: 1
  📝 Changelog entries: 1
  💾 Commits: 1

Next steps:
  1. Review updated YAML files
  2. Complete TODO items in generated snippets
  3. Run '@skill:pr-automation' to create PR
```

## Manual Review Checklist
After doc-sync, developer should:
- [ ] Verify endpoint description is accurate
- [ ] Add error response documentation (400, 401, 404, 422)
- [ ] Update service reference if needed
- [ ] Add example request/response if complex
- [ ] Remove "Auto-generated" notes after review
