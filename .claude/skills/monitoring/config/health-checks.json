{
  "health_checks": [
    {
      "id": "services_status",
      "command": "docker compose ps",
      "threshold": "all_running",
      "severity": "critical",
      "description": "Check all services are running"
    },
    {
      "id": "backend_api",
      "command": "curl -sf http://localhost:8000/health",
      "threshold": "200_ok",
      "severity": "critical",
      "description": "Backend health endpoint check"
    },
    {
      "id": "postgres_ready",
      "command": "docker compose exec -T postgres pg_isready -U familybudget",
      "threshold": "accepting_connections",
      "severity": "critical",
      "description": "PostgreSQL ready check"
    },
    {
      "id": "unhealthy_containers",
      "command": "docker ps --filter \"health=unhealthy\"",
      "threshold": "empty",
      "severity": "high",
      "description": "Find unhealthy containers"
    },
    {
      "id": "backend_errors",
      "command": "docker compose logs --since 1h backend | grep ERROR | wc -l",
      "threshold": "< 10",
      "severity": "medium",
      "description": "Count backend errors in last hour"
    },
    {
      "id": "database_queries",
      "command": "docker compose exec -T postgres psql -U familybudget -c \"SELECT count(*) FROM pg_stat_activity WHERE state = 'active';\"",
      "threshold": "< 50",
      "severity": "medium",
      "description": "Active database queries count"
    },
    {
      "id": "disk_space",
      "command": "df -h /opt/budget | tail -1 | awk '{print $5}' | sed 's/%//'",
      "threshold": "< 85",
      "severity": "high",
      "description": "Disk space usage check"
    },
    {
      "id": "memory_usage",
      "command": "free -m | grep Mem | awk '{printf \"%.0f\", ($3/$2)*100}'",
      "threshold": "< 90",
      "severity": "medium",
      "description": "Memory usage percentage"
    },
    {
      "id": "cpu_usage",
      "command": "top -bn1 | grep 'Cpu(s)' | awk '{print $2}' | sed 's/%us,//'",
      "threshold": "< 80",
      "severity": "medium",
      "description": "CPU usage percentage"
    },
    {
      "id": "network_connectivity",
      "command": "docker compose exec -T backend ping -c 1 postgres",
      "threshold": "0_packet_loss",
      "severity": "high",
      "description": "Network connectivity to PostgreSQL"
    }
  ],
  "metadata": {
    "version": "2.1.0",
    "last_updated": "2026-01-24",
    "description": "Health checks for Family Budget monitoring with TOON optimization"
  },
  "toon": {
    "health_checks_toon": "health_checks[10]{id,command,threshold,severity,description}:\n  services_status,docker compose ps,all_running,critical,Check all services are running\n  backend_api,curl -sf http://localhost:8000/health,200_ok,critical,Backend health endpoint check\n  postgres_ready,docker compose exec -T postgres pg_isready -U familybudget,accepting_connections,critical,PostgreSQL ready check\n  unhealthy_containers,\"docker ps --filter \\\"health=unhealthy\\\"\",empty,high,Find unhealthy containers\n  backend_errors,docker compose logs --since 1h backend | grep ERROR | wc -l,< 10,medium,Count backend errors in last hour\n  database_queries,\"docker compose exec -T postgres psql -U familybudget -c \\\"SELECT count(*) FROM pg_stat_activity WHERE state = 'active';\\\"\",< 50,medium,Active database queries count\n  disk_space,df -h /opt/budget | tail -1 | awk '{print $5}' | sed 's/%//',< 85,high,Disk space usage check\n  memory_usage,\"free -m | grep Mem | awk '{printf \\\"%.0f\\\", ($3/$2)*100}'\",< 90,medium,Memory usage percentage\n  cpu_usage,\"top -bn1 | grep 'Cpu(s)' | awk '{print $2}' | sed 's/%us,//'\",< 80,medium,CPU usage percentage\n  network_connectivity,docker compose exec -T backend ping -c 1 postgres,0_packet_loss,high,Network connectivity to PostgreSQL",
    "token_savings": "46.4%",
    "size_comparison": {
      "json_tokens": 580,
      "toon_tokens": 311,
      "saved_tokens": 269,
      "saved_percent": "46.4%"
    }
  }
}
