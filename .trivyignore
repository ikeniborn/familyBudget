# Trivy Security Exceptions
# Generated: 2026-02-02
# Review quarterly: Recheck for available fixes

# ============================================================================
# Python Packages (No public fix available)
# ============================================================================

# CVE-2024-23342: python-ecdsa vulnerable to Minerva timing attack
# Status: affected (no fix released as of 2026-02-02)
# Impact: Timing attack on ECDSA signature generation
# Mitigation: Accept risk - low exploitability in our use case (JWT signing)
# Required by: python-jose[cryptography]==3.4.0
# Next review: 2026-05-02
CVE-2024-23342

# ============================================================================
# Debian Packages (will_not_fix or pending upstream fix)
# ============================================================================

# CVE-2023-45853: zlib integer overflow in zipOpenNewFileInZip4_6
# Status: will_not_fix (Debian security team marked as unfixable)
# Impact: Heap buffer overflow in specific zip operations
# Mitigation: Accept risk - application doesn't use vulnerable zip functions
CVE-2023-45853

# CVE-2023-2953: openldap null pointer dereference in ber_memalloc_x
# Status: No fix available in Debian 12 (bookworm)
# Impact: DoS via null pointer dereference
# Mitigation: Accept risk - LDAP functionality not used by application
CVE-2023-2953

# CVE-2025-7458: SQLite integer overflow (CRITICAL)
# Status: Pending fix in Debian 12 (bookworm)
# Impact: Integer overflow in SQLite
# Mitigation: Accept risk temporarily - application uses PostgreSQL, SQLite only as transitive dependency
# Next review: 2026-03-02 (monthly check for Debian security update)
CVE-2025-7458

# CVE-2026-0861: glibc integer overflow in memalign (HIGH)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2026-01-XX
# Impact: Heap corruption via integer overflow
# Mitigation: Accept risk temporarily - very recent CVE, awaiting Debian security update
# Next review: 2026-03-02
CVE-2026-0861

# CVE-2026-24882: GnuPG stack buffer overflow in tpm2daemon (HIGH)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2026-01-XX
# Impact: Arbitrary code execution via stack overflow
# Mitigation: Accept risk temporarily - GPG TPM daemon not used by application
# Next review: 2026-03-02
CVE-2026-24882

# ============================================================================
# New Vulnerabilities (Added 2026-02-03)
# ============================================================================

# CVE-2026-24515: libexpat null pointer dereference (CRITICAL)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2026-01-XX
# Impact: Null pointer dereference → DoS
# Package: libexpat1 v2.5.0-1+deb12u2
# Mitigation: Accept risk temporarily - application uses XML parsing minimally
# Risk assessment: MEDIUM (libexpat used for XML operations)
# Next review: 2026-03-03 (weekly check)
CVE-2026-24515

# CVE-2026-25210: libexpat integer overflow in buffer reallocation (HIGH)
# Status: affected in Debian 12 (bookworm) - CVE published 2026-01-30, fixed in libexpat 2.7.4
# Impact: Information disclosure and data integrity issues via integer overflow in doContent()
# CVSS: 7.8 (NVD) / 6.9 (RedHat) - requires local access (AV:L), low privileges (PR:L)
# Package: libexpat1 v2.5.0-1+deb12u2
# Fixed in: libexpat 2.7.4 (not yet available in Debian 12 bookworm)
# Mitigation: Accept risk temporarily - application uses XML only as transitive dependency
#   - No direct XML file uploads accepted from users
#   - Exploitability reduced by local-access-only requirement (AV:L)
# Next review: 2026-04-11 (monthly check for Debian security update)
CVE-2026-25210

# CVE-2025-13836: cpython excessive read buffering DoS in http.client (CRITICAL)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2025-12-XX
# Impact: DoS via excessive read buffering in http.client module
# Package: libpython3.11-minimal, libpython3.11-stdlib, python3.11-minimal v3.11.2-6+deb12u6
# Mitigation:
#   - Rate limiting enabled (SlowAPI: 100 req/min per IP)
#   - Nginx connection limits (client_body_timeout 30s, client_max_body_size 50M)
#   - Monitoring for abnormal memory usage (psutil alerts)
# Risk assessment: HIGH (FastAPI uses http.client internally via uvicorn)
# Next review: 2026-03-03 (weekly check - HIGH priority)
CVE-2025-13836

# CVE-2025-15366: cpython IMAP command injection (HIGH)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2025-12-XX
# Impact: Command injection in IMAP operations
# Package: Python 3.11 standard library
# Mitigation: Accept risk - application does NOT use IMAP (imaplib module unused)
# Risk assessment: LOW (functionality not used)
# Next review: 2026-04-03 (quarterly check)
CVE-2025-15366

# CVE-2025-15367: cpython POP3 command injection (HIGH)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2025-12-XX
# Impact: Command injection in POP3 operations
# Package: Python 3.11 standard library
# Mitigation: Accept risk - application does NOT use POP3 (poplib module unused)
# Risk assessment: LOW (functionality not used)
# Next review: 2026-04-03 (quarterly check)
CVE-2025-15367

# CVE-2025-8194: cpython infinite loop in tarfile parsing (HIGH)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2025-08-XX
# Impact: DoS via infinite loop when parsing malicious tarfile
# Package: Python 3.11 standard library
# Mitigation:
#   - Input validation: File upload restricted to .xlsx, .csv, .json (NO tar files accepted)
#   - File type whitelist enforced in backend/app/api/v1/endpoints/import_endpoints.py
#   - Max file size limit: 50 MB (prevents large malicious archives)
# Risk assessment: LOW (tar uploads not supported)
# Next review: 2026-04-03 (quarterly check)
CVE-2025-8194

# CVE-2026-1299: cpython email header injection via unquoted newlines (HIGH)
# Status: Pending fix in Debian 12 (bookworm) - CVE published 2026-01-XX
# Impact: Email header injection allowing spam/phishing via unquoted newlines
# Package: Python 3.11 standard library (email module)
# Mitigation:
#   - Email sending via python-telegram-bot (NOT stdlib email module)
#   - User input sanitized before email generation (FastAPI validation)
#   - No direct user control over email headers
# Risk assessment: MEDIUM (email functionality exists, but controlled)
# Next review: 2026-03-03 (weekly check)
CVE-2026-1299

# ============================================================================
# New Vulnerabilities (Added 2026-04-18)
# ============================================================================

# CVE-2025-69720: ncurses buffer overflow → arbitrary code execution (HIGH)
# Status: affected in Debian 12 (bookworm) - no fix available
# Impact: Buffer overflow in ncurses terminal library → arbitrary code execution
# Package: libncursesw6, libtinfo6 v6.4-4
# Mitigation: Accept risk - ncurses is a transitive dependency; application is a
#   web API with no terminal interaction; attack surface limited to local access
# Risk assessment: LOW (terminal library, no user-facing exposure in web API)
# Next review: 2026-07-18 (quarterly)
CVE-2025-69720

# CVE-2026-6100: Python use-after-free in decompression → code execution / info disclosure (HIGH)
# Status: affected in Debian 12 (bookworm) - no fix available in 3.11.2-6+deb12u6
# Impact: Arbitrary code execution or information disclosure via use-after-free in decompression
# Package: libpython3.11-minimal, libpython3.11-stdlib, python3.11-minimal v3.11.2-6+deb12u6
# Mitigation: Accept risk temporarily - awaiting Debian security update (DSA)
#   - File upload restricted to .xlsx, .csv, .json (controlled decompression surface)
#   - FastAPI input validation + size limits (50 MB max)
#   - No untrusted decompression paths from external users
# Risk assessment: MEDIUM (Python runtime, but controlled input paths)
# Next review: 2026-05-18 (monthly - HIGH priority)
CVE-2026-6100

# ============================================================================
# Review Schedule (Updated 2026-04-18)
# ============================================================================
# Weekly reviews: CVE-2025-13836, CVE-2026-24515, CVE-2026-1299 (HIGH/CRITICAL risk)
# Monthly reviews: CVE-2025-7458, CVE-2026-0861, CVE-2026-24882, CVE-2026-25210 (libexpat),
#   CVE-2026-6100 (python decompression use-after-free)
# Quarterly reviews:
#   - CVE-2024-23342 (ecdsa), CVE-2023-45853 (zlib), CVE-2023-2953 (ldap)
#   - CVE-2025-15366 (IMAP), CVE-2025-15367 (POP3), CVE-2025-8194 (tarfile)
#   - CVE-2025-69534 (python-markdown DoS, not installed)
#   - CVE-2025-69720 (ncurses buffer overflow)
#
# Action items:
# 1. Check Debian security tracker weekly for HIGH/CRITICAL CVE patches:
#    - https://security-tracker.debian.org/tracker/CVE-2025-13836
#    - https://security-tracker.debian.org/tracker/CVE-2026-24515
#    - https://security-tracker.debian.org/tracker/CVE-2026-1299
# 2. Monitor application logs for anomalies related to:
#    - Memory usage spikes (CVE-2025-13836)
#    - XML parsing errors (CVE-2026-24515)
# 3. Upgrade Python 3.11 packages when Debian releases security update (DSA)
# 4. Consider upgrade to Python 3.12+ if patches delayed beyond 2026-04-01

# CVE-2026-26007: cryptography SECT curves subgroup validation (HIGH)
# Package: cryptography 46.0.4 (transitive via python-jose[cryptography]==3.4.0)
# Fixed in: cryptography 46.0.5
# Impact: Subgroup attack on SECT curves - leak of private key LSBs, ECDSA forgery
# Risk: LOW for this app (we don't use SECT curves, only NIST P-256/P-384/P-521)
# Action: Upgrade python-jose or pin cryptography>=46.0.5 when fix is stable
# Review: Weekly (security patches)
CVE-2026-26007

# ============================================================================
# New Vulnerabilities (Added 2026-05-15)
# ============================================================================

# CVE-2026-45186: libexpat DoS via attribute name collision complexity (HIGH)
# Status: affected in Debian 12 (bookworm) - CVE published 2026-05-10, no DSA yet
# Impact: DoS via algorithmic complexity in attribute name collision checks (CWE-407)
# CVSS: 7.5 (NVD) AV:N/AC:L/PR:N/UI:N/C:N/I:N/A:H
# Package: libexpat1 v2.5.0-1+deb12u2
# Fixed in: libexpat 2.8.1 (not yet in Debian 12 bookworm)
# Mitigation: Accept risk temporarily - application uses XML only as transitive dependency
#   - No direct XML uploads accepted from users (whitelist: .xlsx, .csv, .json)
#   - Rate limiting via SlowAPI (100 req/min per IP) reduces DoS exposure
#   - Nginx body size limit 50 MB caps payload
# Risk assessment: LOW (no user-controlled XML parsing paths)
# Next review: 2026-06-15 (monthly check for Debian DSA)
CVE-2026-45186

# CVE-2025-69534: python-markdown DoS via malformed HTML-like sequences (HIGH)
# Status: affected in Debian 12 (bookworm) - CVE published 2026-03-05, no DSA yet
# Impact: AssertionError in html.parser.HTMLParser when parsing malformed Markdown → application crash (DoS)
# CVSS: 8.2 (RedHat) / 5.5 (GHSA)
# Package: libpython3.11-minimal, libpython3.11-stdlib, python3.11-minimal v3.11.2-6+deb12u6
# Fixed in: python-markdown 3.8.1 (not yet available in Debian 12 bookworm)
# Mitigation: Accept risk - python-markdown library is NOT installed in this image
#   - Package absent from backend/requirements.txt
#   - No import of markdown module in application code
#   - Note: if markdown is added to requirements.txt in future, pin python-markdown>=3.8.1
# Risk assessment: NEGLIGIBLE (vulnerable library not installed)
# Next review: 2026-06-09 (quarterly)
CVE-2025-69534

# ============================================================================
# New Vulnerabilities (Added 2026-05-29)
# ============================================================================

# CVE-2026-7210: Python xml.parsers.expat / xml.etree.ElementTree insufficient entropy (CRITICAL)
# Status: affected in Debian 12 (bookworm) - no fix available in 3.11.2-6+deb12u6 (FixedVersion=null in Trivy DB)
# Impact: Insufficient entropy in XML parser state → potential XML hash collision / DoS
# Package: libpython3.11-minimal, libpython3.11-stdlib, python3.11-minimal v3.11.2-6+deb12u6
# Mitigation: Accept risk - distroless base image, no apt access for patching
#   - No untrusted XML parsed from external users (whitelist: .xlsx via openpyxl, .csv, .json)
#   - openpyxl handles XLSX XML internally with size limits enforced by FastAPI (50 MB)
#   - SlowAPI rate limiting (100 req/min per IP) caps abuse
# Risk assessment: MEDIUM (controlled XML paths only; await Debian DSA or Python 3.12+ migration)
# Next review: 2026-06-29 (monthly check for Debian DSA on python3.11)
CVE-2026-7210

# ============================================================================
# New Vulnerabilities (Added 2026-05-30)
# ============================================================================

# CVE-2025-59375: libexpat large dynamic memory allocation via small XML document (HIGH)
# Status: affected in Debian 12 (bookworm) - no fix available (FixedVersion=null in Trivy DB)
# Impact: Attacker-triggered large dynamic memory allocations from small input → DoS / OOM
# Package: libexpat1 v2.5.0-1+deb12u2
# Mitigation: Accept risk temporarily - same package as CVE-2026-24515 / CVE-2026-25210 / CVE-2026-45186
#   - Application uses XML only transitively (openpyxl XLSX parsing)
#   - No direct XML uploads accepted from users
#   - FastAPI 50 MB body limit caps allocation surface
#   - SlowAPI rate limiting (100 req/min per IP) caps abuse
# Risk assessment: MEDIUM (controlled XML paths only)
# Next review: 2026-06-30 (monthly check for Debian DSA on libexpat1)
CVE-2025-59375

# ============================================================================
# New Vulnerabilities (Added 2026-06-05)
# ============================================================================

# CVE-2026-3644: cpython incomplete control character validation in http.cookies (HIGH)
# Status: affected in Debian 12 (bookworm) - no fix available in 3.11.2-6+deb12u6 (FixedVersion=null in Trivy DB)
# Impact: Cookie header injection via incomplete control character validation in http.cookies
# Package: libpython3.11-minimal, libpython3.11-stdlib, python3.11-minimal v3.11.2-6+deb12u6
# Mitigation: Accept risk temporarily - awaiting Debian DSA on python3.11
#   - FastAPI/Starlette manages cookie set/parse paths (not stdlib http.cookies directly)
#   - Auth cookies issued server-side with HttpOnly, Secure, SameSite=Lax
#   - SlowAPI rate limiting (100 req/min per IP) caps abuse
# Risk assessment: MEDIUM (cookie surface controlled by framework, not stdlib)
# Next review: 2026-07-05 (monthly check for Debian DSA on python3.11)
CVE-2026-3644

# CVE-2026-4224: cpython stack overflow parsing XML with deeply nested DTD content models (HIGH)
# Status: affected in Debian 12 (bookworm) - no fix available in 3.11.2-6+deb12u6 (FixedVersion=null in Trivy DB)
# Impact: Stack overflow → DoS via deeply nested DTD content models in XML parser
# Package: libpython3.11-minimal, libpython3.11-stdlib, python3.11-minimal v3.11.2-6+deb12u6
# Mitigation: Accept risk - same XML path controls as CVE-2026-7210 / CVE-2025-59375
#   - No untrusted XML parsed from external users (whitelist: .xlsx via openpyxl, .csv, .json)
#   - openpyxl handles XLSX XML internally with size limits (50 MB FastAPI body cap)
#   - DTD processing disabled by default in stdlib XML modules used transitively
# Risk assessment: LOW (no user-controlled XML/DTD parsing paths)
# Next review: 2026-07-05 (monthly check for Debian DSA on python3.11)
CVE-2026-4224
