# FastAPI Framework
fastapi==0.121.2
starlette>=0.49.1  # Security fix for CVE-2025-54121, CVE-2025-62727
uvicorn[standard]==0.30.6
jinja2==3.1.6
aiofiles==24.1.0
httpx>=0.27.0  # HTTP client for avatar fetching, notifications, Telegram API

# Database
sqlmodel==0.0.22
asyncpg==0.29.0
psycopg2-binary==2.9.10  # Required for Alembic migrations (sync driver)
alembic==1.14.0

# Redis (Caching & Pub/Sub)
redis>=5.0.0

# JSON Performance
orjson>=3.10.0  # High-performance JSON serialization (3-10x faster)

# Authentication & Security
python-jose[cryptography]==3.4.0
python-multipart==0.0.27
slowapi==0.1.9             # Rate limiting for API endpoints (brute-force protection)
argon2-cffi==23.1.0        # Password hashing (Argon2 - winner of Password Hashing Competition)
pyotp==2.9.0               # TOTP/2FA generation and verification (Google Authenticator compatible)
email-validator==2.2.0     # Email validation for Pydantic EmailStr
webauthn==2.1.0            # WebAuthn biometric authentication (TouchID/FaceID)

# Configuration
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Logging
python-json-logger==3.2.1

# Monitoring
psutil==6.1.1
docker==7.1.0  # Docker SDK for collecting container logs

# Background Jobs
apscheduler==3.11.0

# Push Notifications (Web Push / VAPID)
pywebpush==2.0.0
