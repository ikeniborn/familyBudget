# FastAPI Framework
fastapi==0.121.2
starlette>=0.49.1  # Security fix for CVE-2025-54121, CVE-2025-62727
uvicorn[standard]==0.30.6
jinja2==3.1.6
aiofiles==24.1.0

# Database
sqlmodel==0.0.22
asyncpg==0.29.0
psycopg2-binary==2.9.10  # Required for Alembic migrations (sync driver)
alembic==1.14.0

# Redis (Caching & Pub/Sub)
redis>=5.0.0

# JSON Performance
orjson>=3.10.0  # High-performance JSON serialization (3-10x faster)

# Authentication & Security
python-jose[cryptography]==3.4.0
ecdsa>=0.20.0              # Security fix for CVE-2024-23342 (Minerva attack vulnerability)
python-multipart==0.0.22
slowapi==0.1.9             # Rate limiting for API endpoints (brute-force protection)
argon2-cffi==23.1.0        # Password hashing (Argon2 - winner of Password Hashing Competition)
pyotp==2.9.0               # TOTP/2FA generation and verification (Google Authenticator compatible)
email-validator==2.2.0     # Email validation for Pydantic EmailStr
webauthn==2.1.0            # WebAuthn biometric authentication (TouchID/FaceID)

# Configuration
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Logging
python-json-logger==3.2.1

# Monitoring
psutil==6.1.1
docker==7.1.0  # Docker SDK for collecting container logs

# Background Jobs
apscheduler==3.11.0

# Export functionality
openpyxl==3.1.5        # Excel (xlsx) generation
reportlab==4.2.5       # PDF generation

# Push Notifications (Web Push / VAPID)
pywebpush==2.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1

# Development
black==24.10.0
ruff==0.8.4
sse-starlette>=2.0.0

# Security: Transitive dependency fixes
jaraco.context>=6.1.0      # Security fix for CVE-2026-23949 (path traversal vulnerability)
wheel>=0.46.2              # Security fix for CVE-2026-24049 (privilege escalation vulnerability)
