# Core FastAPI
fastapi[all]~=0.111.0  # Includes uvicorn, pydantic, starlette, etc.

# Database
sqlalchemy[asyncio]~=2.0.29 # ORM with asyncio support
psycopg2-binary~=2.9.9    # PostgreSQL adapter (binary for easier install)
alembic~=1.13.1          # Database migrations

# Authentication & Security
python-jose[cryptography]~=3.3.0 # JWT handling
passlib[bcrypt]~=1.7.4         # Password hashing

# Environment Variables
python-dotenv~=1.0.1

# Optional: For Telegram Login validation (if needed beyond basic checks)
# requests~=2.31.0 # Or another HTTP client if making external calls

# Development/Testing (Optional - can be in a separate requirements-dev.txt)
# pytest~=8.1.1
# httpx~=0.27.0 # For testing async API calls