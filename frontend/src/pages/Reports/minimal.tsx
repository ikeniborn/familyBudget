import React from 'react';

const MinimalReportsPage: React.FC = () => {
  return (
    <div>
      <h1>Minimal Reports Page</h1>
      <p>This is a minimal version to test if React routing works.</p>
    </div>
  );
};

export default MinimalReportsPage;