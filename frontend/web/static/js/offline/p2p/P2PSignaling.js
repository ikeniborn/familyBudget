/**
 * P2PSignaling - DEPRECATED.
 * QR-based SDP exchange has been removed in v1.2.0.
 * SDP encoding/decoding for relay transport is now inlined in P2PUIController.js.
 *
 * This file is retained as an empty module to avoid 404 errors from any
 * cached service workers or old imports that may still reference it.
 * It will be fully removed in the next cleanup pass.
 *
 * @deprecated since v1.2.0 — use relay channel via P2PUIController instead
 */

export {};
