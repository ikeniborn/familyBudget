version: '2.4'

services:
  postgres:
    container_name: postgres
    image: "postgres:13"
    networks:
      app_network:
        ipv4_address: 10.5.0.2
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 4phDukPGF7sUWSEvfycX
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G

  api:
    container_name: api
    build:
      dockerfile: ./app/api/Dockerfile
      context: ./
    volumes:
      - /home/ikeni/Documents/Git/familyBudget/web/app/api:/usr/src/app:delegated
    ports:
      - "8888:8888"
    command: "uvicorn api:app --host 0.0.0.0 --port 8888 --reload"
    environment:
      - USER_ID=1000
      - GROUP_ID=1000
      - POSTGRES_HOST=10.5.0.2
      - POSTGRES_PORT=5432
      - BUDGET_POSTGRES_DB=budgetdb
      - BUDGET_POSTGRES_USER=budget
      - BUDGET_POSTGRES_PASSWORD=rEWbdYxog7azBce2fA4D
      - PORTFOLIO_POSTGRES_DB=portfoliodb
      - PORTFOLIO_POSTGRES_USER=portfolio
      - PORTFOLIO_POSTGRES_PASSWORD=WRHUT86xXzcghBmSPpFd
      # - POLARS_SKIP_CPU_CHECK=false
    networks:
      app_network:
        ipv4_address: 10.5.0.3
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
    healthcheck:
      test: ["CMD", "bash", "-c", "echo -n '' > /dev/tcp/0.0.0.0/80"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: always
    depends_on:
      - postgres

  budget-ui:
    container_name: budget-ui
    build:
      dockerfile: ./app/budget/Dockerfile
      context: ./
    volumes:
      - /home/ikeni/Documents/Git/familyBudget/web/app/budget:/usr/src/app:delegated
    ports:
      - "8501:8501"
    command: "streamlit run ui.py --server.port=8501"
    environment:
      - USER_ID=1000
      - GROUP_ID=1000
      - API_URL=http://10.5.0.3:8888
      - POLARS_SKIP_CPU_CHECK=false
    networks:
      app_network:
        ipv4_address: 10.5.0.4
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
    healthcheck:
      test: ["CMD", "curl","--fail","http://0.0.0.0:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: always
    depends_on:
      - api

  # portfolio-ui:
  #   container_name: portfolio-ui
  #   build:
  #     dockerfile: ./app/budget/Dockerfile
  #     context: ./
  #   volumes:
  #     - /home/ikeni/Documents/Git/familyBudget/web/app/portfolio:/usr/src/app:delegated
  #   ports:
  #     - "8502:8502"
  #   command: "streamlit run ui.py --server.port=8502"
  #   environment:
  #     - USER_ID=1000
  #     - GROUP_ID=1000
  #     - API_URL=http://10.5.0.3:8888
  #     - POLARS_SKIP_CPU_CHECK=false
  #   networks:
  #     app_network:
  #       ipv4_address: 10.5.0.5
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: '1'
  #         memory: 1G
  #   healthcheck:
  #     test: ["CMD", "curl","--fail","http://0.0.0.0:8501/_stcore/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5
  #     start_period: 30s
  #   restart: always
  #   depends_on:
  #     - api

  haproxy:
    container_name: haproxy
    image: "haproxy:2.2-alpine"
    ports:
      - "80:80"
    volumes:
      - /home/ikeni/Documents/Git/familyBudget/web/services/haproxy/haproxy-dev.cfg:/usr/local/etc/haproxy/haproxy.cfg:delegated
    networks:
      app_network:
        ipv4_address: 10.5.0.10
    depends_on:
      - budget-ui
      # - portfolio-ui

  pgadmin:
    container_name: pgadmin
    image: elestio/pgadmin:REL-8_5
    environment:
      PGADMIN_DEFAULT_EMAIL: ikeniborn@gmail.com
      PGADMIN_DEFAULT_PASSWORD: JxUdSobKjAVrB2Wm4Li5
      PGADMIN_CONFIG_SERVER_MODE: "False"
      # PGADMIN_LISTEN_PORT: 80
    ports:
      - "5050:80"
    volumes:
      - pgadmin-data:/var/lib/pgadmin
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
    networks:
      app_network:
        ipv4_address: 10.5.0.7
    depends_on:
      - postgres

networks:
  app_network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.0.0/16
          gateway: 10.5.0.1

volumes:
  postgres-data:
    driver: local
  pgadmin-data:
    driver: local

